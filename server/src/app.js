const express = require("express");

const app = express();

app.listen(3000, ()=>{
    console.log("Server running on port 3000 XD");
});git remote add origin https://github.com/AnaO-97/RedSocial.git